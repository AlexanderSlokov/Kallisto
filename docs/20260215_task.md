# Kallisto Server - Envoy-Style Architecture

## Phase 2.1: Protocol Buffer & gRPC Setup
- [x] Create `proto/kallisto.proto` - Service definitions (vcpkg)
- [x] Implement `gRPC Handler` (Service Implementation)
- [x] Benchmark gRPC Server (Basic)
- [x] Create `vcpkg.json` - Dependency manifest
- [x] Update `CMakeLists.txt` - Add gRPC/Protobuf/simdjson deps
- [x] vcpkg install complete (all 12 packages cached)

## Phase 2.2: Network Infrastructure (SO_REUSEPORT)
- [x] Create `include/kallisto/net/listener.hpp`
- [x] Create `src/net/listener.cpp`
- [x] Add `bindListener()` to Worker interface + implementation
- [x] Fix linker: moved `listener.cpp` to `kallisto_lib`
- [x] Unit test: `test_listener` — 4/4 passed ✅

## Phase 2.3: gRPC Server Integration
- [x] Create `include/kallisto/server/grpc_handler.hpp`
- [x] Create `src/server/grpc_handler.cpp` (TypedCallData + timer polling)
- [x] Fix: Rewrote CallData with per-RPC typed `ServerAsyncResponseWriter<T>`

## Phase 2.4: HTTP Server (Vault-Compatible API)
- [x] Create `include/kallisto/server/http_handler.hpp`
- [x] Create `src/server/http_handler.cpp`
- [x] Fix: `SecretEntry.timestamp` → `created_at` (chrono)

## Phase 2.5: Main Server Entry Point
- [x] Create `src/kallisto_server.cpp`
- [x] Update `Makefile` with server targets
- [x] Fix: `kallisto::info` scope in signal handler

## Phase 2.6: Build & Verify
- [x] Full server build passes (100%) ✅
- [x] `test_listener` — 4/4 SO_REUSEPORT tests passed ✅
- [x] Server starts with 4 workers, HTTP:8200, gRPC:8201 ✅
- [x] HTTP API: PUT/GET/DELETE/404 all working ✅
- [ ] gRPC API test (needs grpcurl)
- [x] Load testing (bench_multithread reached 2.8M RPS) ✅

## Phase 2.7: Documentation
- [x] Update README with HOW TO USE section
- [x] Update README with Server Architecture (Envoy/Sharding)
- [x] Update Experimental Results with latest benchmarks

## Phase 2.8: Server Load Testing (wrk)
- [x] Install wrk
- [x] Create Lua scripts (GET/PUT/MIXED)
- [x] Create orchestration script (bench/run_server_bench.sh)
- [x] Add Makefile targets (`bench-server`)
- [x] Run benchmarks and collect results (Stable at c=50)

## Phase 2.9: Stability & Optimization
- [x] Fix Dispatcher use-after-free (Option C: unique_ptr + deferred mutation)
- [x] Optimize Release Build (-O3)
- [x] Verify Server Stability under load (SIGTERM handling invalid)
- [x] Research Envoy gRPC Architecture (Confirmed `FileEvent` pattern, missing .cc files)

## Phase 3: Optimizations (Active)
- [x] **Envoy-Style gRPC**: Attempted dedicated thread bridge (Result: Low RPS due to syscall storm).
- [x] **Hybrid Polling**: Optimizating gRPC with "Check-Before-Sleep" + `SO_REUSEPORT`.
- [x] **Direct Execution**: Remove Dispatcher bridge entirely. Result: **5.8k RPS** (Stable, Environment Limited).
- [ ] **Security**: Integrate AES-256 for RocksDB.
- [ ] **Replication**: Research `NuRaft`.
